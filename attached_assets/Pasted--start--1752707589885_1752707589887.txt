Пользовательский интерфейс и команды
 Чистый чат:
Все команды и промпты (ввод ФИО, /start, ручные ответы) удаляются, чтобы в чате оставались только статусные сообщения и актуальное меню.

 Меню всегда "внизу":
После любого сообщения или отправки файла появляется главное или контекстное меню сразу под этим сообщением.

 Подсказки при вводе:
Каждый этап ручного ввода сопровождается подсказкой (пример, формат, пояснение).

 2-3 столбца для кнопок:
Все большие меню (локации, ЛС, командиры, экспорт) оформлены в 2–3 столбца для красоты и удобства.

 Единая честность интерфейса:
Все тексты на кнопках строго совпадают с описаниями в подсказках и инструкциях.

II. 🛡️ Защита от дурака и валидация на каждом этапе
 Ввод ФИО и регистрация:

Не принимаются пустые, слишком короткие или бессмысленные строки (“ААА”, “123”, “.”).

Проверка: минимум две части (фамилия и инициалы), только кириллица, минимум 5 символов.

Всегда есть пример (“Иванов И.И.”).

Без корректного ФИО — пользователь не получает меню.

 Добавление/редактирование пользователей:

Запрет добавлять бойца/командира с уже существующим ФИО или Telegram ID.

Если такой пользователь есть — появляется предупреждение.

При превышении лимита пользователей (если он задан) — блокировать добавление.

 Удаление бойца/командира:

Всегда двойное подтверждение (“Вы уверены? Это действие необратимо!”).

Запретить удаление себя или последнего администратора/командира.

 Кнопки "Убыл"/"Прибыл":

Запретить двойные/логически неверные действия (например, нельзя “Убыл” два раза подряд).

Для “Убыл” обязательно выбирать локацию. Если нужна заметка — нельзя оставлять поле пустым (или явная кнопка “Пропустить”).

Если уже в нужном статусе — появляется сообщение “Уже отмечено”.

 Управление правами и настройками:

Запрет отключения PUSH для всех командиров (должен остаться хотя бы 1 с включённым PUSH).

Запрет скрытия всех из отчётов (хотя бы 1 show_in_reports=1).

Все поля и флаги — валидация при каждом изменении.

 Экспорт и журнал:

Перед экспортом — проверка наличия данных (иначе: “Нет данных для экспорта”).

Если экспорт превышает N строк — предупреждение/разбивка.

 Уведомления и рассылки:

Не отправлять уведомления “мертвым” аккаунтам.

Проверка лимитов Telegram (анти-флуд).

 Кнопки и меню:

При нажатии устаревшей кнопки — сообщение “Меню устарело, выберите снова”.

Любой ручной ввод — с валидацией и информативной ошибкой.

Любое действие всегда можно отменить кнопкой “Назад/Отмена”.

 Массовые действия (“Опасная зона”):

Для “отметить всех” — подтверждение с набором текста “Да, я уверен”.

III. 🛠️ Админ-панель и права
 Гибкая структура меню:
Логические разделы: 🏠 Главное, 👥 ЛС, 🛡️ Командиры, 📖 Журнал, 📤 Экспорт, ⚙️ Настройки (в т.ч. уведомления).

 Права и управление:

Управление PUSH-правами (can_get_notifications).

Новое право: "Показывать в отчётах" (show_in_reports).

Подтверждение на удаление.

После экспорта — возврат в актуальное меню.

IV. 🔔 Система уведомлений
 PUSH только выбранным админам:
Любая отметка “ПРИБЫЛ/УБЫЛ” — только командирам с включённым PUSH.

 Гибкая настройка получателей и типов событий:
Можно включать/отключать PUSH любому командиру и по отдельным типам событий (прибытие, убытие, сводки, напоминания).

 Режим “не беспокоить”:
Можно временно отключать рассылки для пользователя/группы.

 Расписание авторассылок:
19:00 — вечерний доклад, 20:30 — напоминание неотметившимся (автоматически).

 Креативные напоминания:
Рандомизация из ≥50 вариантов (notifications.json).

 Структурированный формат уведомлений:
Иконки, дата/время, шапка, ключ:значение.

V. 📋 Формат списков и журналов
 Пагинация:
Вывод списков — по 5–10 на страницу, с навигацией.

 Иконки и метки:
Тип пользователя, номера страниц.

 Флаг show_in_reports:
В сводках только отмеченные.

VI. 📝 UX-детали и регистрация
 Регистрация через ФИО:
Без ввода ФИО — меню не показывается.

 Порядок действий:
Сначала собирается заметка, потом фиксируется событие.

 Удаление — только с подтверждением.

 Меню и экспорт — возврат в актуальный раздел.